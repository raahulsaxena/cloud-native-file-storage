version: '3.9'

services:
  file-storage-service:
    build: .
    container_name: file-storage-service
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - /tmp:/tmp
    restart: unless-stopped